FROM debian:bullseye-slim
RUN apt-get update && apt-get install -y  libpcap snmpd \
  && apt-get clean \
  && rm -rf /var/lib/apt/lists/*

COPY twrmon  /sbin/twrmon
RUN  chmod +x /sbin/twrmon
COPY start.sh  /sbin/strat.sh
RUN  chmod +x /sbin/start.sh
ENTRYPOINT [ "/sbin/start.sh" ]

